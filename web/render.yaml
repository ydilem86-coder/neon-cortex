services:
  - type: web
    name: neon-cortex-bot
    env: python
    buildCommand: pip install -r ../requirements.txt -r requirements.txt
    startCommand: python run_web.py $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
